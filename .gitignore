# Per-machine files created on each host from the *.example templates.
# They live inside the stow packages but must never be committed.
shell/.config/shell/local.sh
git/.config/git/local

# Subagent-driven-development scratch (ledger, briefs, review packages).
.superpowers/

# herdr keeps runtime state alongside its config in ~/.config/herdr/. Only
# config.toml is tracked; ignore the sockets, logs, and session state so they
# are never committed and doctor.sh doesn't flag them as drift.
herdr/.config/herdr/*.log
herdr/.config/herdr/*.sock
herdr/.config/herdr/session.json
herdr/.config/herdr/.plugins.lock

# Python bytecode from running the herdr auto-layout daemon or py_compile.
__pycache__/
*.pyc

# Older lazygit wrote its own state (last-used branch, panel sizes) next to
# its config; 0.64, the version this repo requires, writes it under
# $XDG_STATE_HOME instead (~/.local/state/lazygit/state.yml, verified live —
# outside this repo's concern, and not this entry's target). This line is
# kept as harmless insurance in case a future or older lazygit ever does
# write state beside the config again; --no-folding makes ~/.config/lazygit
# a real directory, so if that happens doctor.sh would otherwise flag it as
# drift.
lazygit/.config/lazygit/state.yml
